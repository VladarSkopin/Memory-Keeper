
import 'package:flutter/material.dart';


class TestAlertDialogWithTitle extends StatelessWidget {
  const TestAlertDialogWithTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
